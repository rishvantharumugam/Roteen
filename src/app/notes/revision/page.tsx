import { RevisionNavigation } from '@/navigation/revisionNavigation';

export const metadata = {
  title: 'Revision | Roteen',
  description: 'Organize your playlists and revise smarter.',
};

export default function RevisionRoute() {
  return <RevisionNavigation />;
}

